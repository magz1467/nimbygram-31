{
  "name": "nimbygram",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/ && cp -r public/* dist/"
  },
  "engines": {
    "node": "18.x"
  }
}
